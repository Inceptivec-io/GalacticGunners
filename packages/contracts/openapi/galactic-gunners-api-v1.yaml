openapi: 3.1.0
info:
  title: Galactic Gunners API
  version: 1.0.0
  description: Authoritative v1 interface for Galactic Gunners production architecture foundation.
servers:
  - url: /api/v1
paths:
  /health/:
    get:
      operationId: health
      summary: Service health.
      responses:
        '200':
          description: Backend healthy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /game-runs/:
    post:
      operationId: startGameRun
      summary: Start a pending game run.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartGameRunRequest'
      responses:
        '201':
          description: Game run created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GameRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /game-runs/{runId}/complete/:
    post:
      operationId: completeGameRun
      summary: Complete a pending game run under backend-owned validation.
      parameters:
        - $ref: '#/components/parameters/RunId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScoreSubmission'
      responses:
        '200':
          description: Completion accepted and run validated by foundation policy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompletedGameRun'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /leaderboard/:
    get:
      operationId: listLeaderboard
      summary: List valid published leaderboard entries.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
      responses:
        '200':
          description: Valid published leaderboard entries ordered by score descending and published time ascending.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaderboardResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
  /levels/:
    get:
      operationId: listPublishedLevels
      summary: List public published campaign levels.
      responses:
        '200':
          description: Published level catalogue.
  /levels/{slug}/:
    get:
      operationId: getPublishedLevel
      summary: Resolve the active published version for a level slug.
      parameters:
        - name: slug
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Published level authority. }
        '404': { $ref: '#/components/responses/NotFound' }
  /levels/{slug}/versions/{version}/:
    get:
      operationId: getPublishedLevelVersion
      summary: Resolve one published immutable level version.
      parameters:
        - name: slug
          in: path
          required: true
          schema: { type: string }
        - name: version
          in: path
          required: true
          schema: { type: integer, minimum: 1 }
      responses:
        '200': { description: Published immutable level version. }
  /admin/levels/:
    post:
      operationId: createLevelDraft
      summary: Create a privileged DRAFT level and version.
      responses:
        '201': { description: Draft created. }
        '403': { description: Level administrator required. }
  /admin/levels/import/:
    post:
      operationId: importLevelDraft
      summary: Import a validated DRAFT level only.
      responses:
        '201': { description: Draft imported. }
  /admin/levels/generate/:
    post:
      operationId: generateLevelDraft
      summary: Generate a deterministic DRAFT candidate; this endpoint never publishes.
      responses:
        '201': { description: Draft generated. }
  /admin/levels/{levelId}/drafts/:
    post:
      operationId: saveLevelAuthoringDraft
      summary: Save one checksum-constrained schema 1.1 level draft.
      parameters:
        - name: levelId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LevelDraftSaveRequest'
      responses:
        '201':
          description: Immutable draft version created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LevelVersion'
        '409': { $ref: '#/components/responses/Conflict' }
  /admin/levels/{levelId}/preview/{checksum}:
    get:
      operationId: previewLevelAuthoringDraft
      summary: Resolve one immutable internal preview by exact checksum.
      parameters:
        - name: levelId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: checksum
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-f]{64}$' }
      responses:
        '200':
          description: Exact unranked preview version.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LevelVersion' }
        '404': { $ref: '#/components/responses/NotFound' }
  /portal/organizations/{organizationSlug}/maps/{levelId}/drafts/:
    post:
      operationId: saveOrganizationLevelAuthoringDraft
      summary: Save an organisation-owned checksum-constrained schema 1.1 draft.
      parameters:
        - name: organizationSlug
          in: path
          required: true
          schema: { type: string }
        - name: levelId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/LevelDraftSaveRequest' }
      responses:
        '201':
          description: Immutable organisation draft version created.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LevelVersion' }
        '403': { description: Organisation editor entitlement required. }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /portal/organizations/{organizationSlug}/maps/{levelId}/preview/{checksum}:
    get:
      operationId: previewOrganizationLevelAuthoringDraft
      summary: Resolve one exact tenant-isolated unranked preview.
      parameters:
        - name: organizationSlug
          in: path
          required: true
          schema: { type: string }
        - name: levelId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: checksum
          in: path
          required: true
          schema: { type: string, pattern: '^[0-9a-f]{64}$' }
      responses:
        '200':
          description: Exact tenant-owned preview version.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LevelVersion' }
        '403': { description: Organisation membership and editor entitlement required. }
        '404': { $ref: '#/components/responses/NotFound' }
components:
  parameters:
    RunId:
      name: runId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    BadRequest:
      description: Invalid payload.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Requested resource not found.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Incompatible lifecycle mutation.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    LevelDraftSaveRequest:
      type: object
      additionalProperties: false
      required: [expected_checksum, config]
      properties:
        expected_checksum:
          type: string
          pattern: '^[0-9a-f]{64}$'
        config:
          $ref: '#/components/schemas/LevelAuthoringDocument'
    LevelAuthoringDocument:
      type: object
      additionalProperties: false
      required: [schema_version, id, slug, name, version, status, sequence, seed, canvas, player_spawns, entities, formations, hazard_emitters, shield_structures, drop_rules, objectives, boarding_anchors, gameplay, performance_budget]
      properties:
        schema_version: { const: '1.1' }
        id: { type: string, minLength: 1 }
        slug: { type: string, pattern: '^[a-z0-9-]{3,64}$' }
        name: { type: string, minLength: 1, maxLength: 128 }
        version: { type: integer, minimum: 1 }
        status: { type: string, enum: [DRAFT, VALIDATED, PUBLISHED, SUPERSEDED, ARCHIVED] }
        sequence: { type: integer, minimum: 1 }
        seed: { type: integer }
        canvas: { type: object }
        player_spawns: { type: array, minItems: 1, maxItems: 2 }
        entities: { type: array }
        formations: { type: array }
        hazard_emitters: { type: array }
        shield_structures: { type: array }
        drop_rules: { type: array }
        objectives: { type: array }
        boarding_anchors: { type: array, maxItems: 1 }
        gameplay: { type: object }
        performance_budget: { type: object }
    LevelVersion:
      type: object
      required: [id, version, schema_version, checksum, config, validation_report, status]
      properties:
        id: { type: string, format: uuid }
        version: { type: integer, minimum: 1 }
        schema_version: { type: string, enum: ['1.0', '1.1'] }
        checksum: { type: string, pattern: '^[0-9a-f]{64}$' }
        config: { type: object }
        validation_report: { type: object }
        status: { type: string, enum: [DRAFT, VALIDATED, PUBLISHED, SUPERSEDED, ARCHIVED] }
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status, service, api_version]
      properties:
        status:
          type: string
          const: ok
        service:
          type: string
          const: galactic-gunners-api
        api_version:
          type: string
          const: v1
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [code, detail, errors]
      properties:
        code:
          type: string
          enum: [invalid_request, not_found, conflict, request_failed]
        detail:
          type: string
        errors:
          type: object
          additionalProperties:
            oneOf:
              - type: string
              - type: array
                items: {}
              - type: object
    StartGameRunRequest:
      type: object
      additionalProperties: false
      required: [game_version, client_type]
      properties:
        game_version:
          type: string
          minLength: 1
          maxLength: 32
        client_type:
          $ref: '#/components/schemas/ClientType'
    GameRun:
      type: object
      additionalProperties: false
      required:
        - id
        - game_version
        - client_type
        - started_at
        - completed_at
        - score
        - level_reached
        - lives_used
        - nukes_used
        - victory
        - validity
      properties:
        id:
          type: string
          format: uuid
        game_version:
          type: string
          minLength: 1
          maxLength: 32
        client_type:
          $ref: '#/components/schemas/ClientType'
        started_at:
          type: string
          format: date-time
        completed_at:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
        score:
          type: integer
          minimum: 0
        level_reached:
          type: string
          maxLength: 32
        lives_used:
          type: integer
          minimum: 0
        nukes_used:
          type: integer
          minimum: 0
        victory:
          type: boolean
        validity:
          $ref: '#/components/schemas/GameRunValidity'
    CompletedGameRun:
      type: object
      additionalProperties: false
      required: [id, score, level_reached, lives_used, nukes_used, victory, validity, completed_at]
      properties:
        id:
          type: string
          format: uuid
        score:
          type: integer
          minimum: 0
        level_reached:
          type: string
          maxLength: 32
        lives_used:
          type: integer
          minimum: 0
        nukes_used:
          type: integer
          minimum: 0
        victory:
          type: boolean
        validity:
          $ref: '#/components/schemas/GameRunValidity'
        completed_at:
          type: string
          format: date-time
    ScoreSubmission:
      type: object
      additionalProperties: false
      required: [claimed_score, level_reached, lives_used, nukes_used, victory, event_summary]
      properties:
        claimed_score:
          type: integer
          minimum: 0
        level_reached:
          type: string
          maxLength: 32
        lives_used:
          type: integer
          minimum: 0
        nukes_used:
          type: integer
          minimum: 0
        victory:
          type: boolean
        event_summary:
          type: object
        payload_hash:
          type: string
          pattern: '^[0-9a-fA-F]{64}$'
        idempotency_key:
          anyOf:
            - type: string
              minLength: 1
              maxLength: 128
            - type: 'null'
    LeaderboardEntry:
      type: object
      additionalProperties: false
      required: [run_id, display_name, score, published_at]
      properties:
        run_id:
          type: string
          format: uuid
        display_name:
          type: string
          minLength: 1
          maxLength: 64
        score:
          type: integer
          minimum: 0
        published_at:
          type: string
          format: date-time
    LeaderboardResponse:
      type: object
      additionalProperties: false
      required: [total, results]
      properties:
        total:
          type: integer
          minimum: 0
        results:
          type: array
          items:
            $ref: '#/components/schemas/LeaderboardEntry'
    ClientType:
      type: string
      enum: [web, windows, macos, android, ios, unknown]
    GameRunValidity:
      type: string
      enum: [pending, valid, rejected]

# Galactic Gunners Contracts

Machine-readable contracts shared between the web client, game core and Django backend.

- `openapi/` — HTTP API contract.
- `schemas/` — event and data payload schemas.

Contracts are versioned by product interface version, not handoff/sprint sequence.

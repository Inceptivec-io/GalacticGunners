# Contract Execution Contract

Scope: `packages/contracts/` only.

Contracts are machine-readable product interfaces, not informal examples. A breaking contract change requires explicit version/migration/deprecation treatment. Stable semantic names only; no work-sequence IDs.

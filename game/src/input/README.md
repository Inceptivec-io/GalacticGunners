# Input

Keyboard, touch and controller inputs are normalised behind stable product actions. Provider-specific button identities must not leak through gameplay logic where a semantic action is available.

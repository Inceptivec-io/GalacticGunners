# Scenes

Production Phaser scenes are migrated here one bounded behaviour-preserving movement at a time. Scene names describe product meaning (`MainMenuScene`, `LevelOneScene`, `BossScene`, `VictoryScene`, `GameOverScene`) and never execution sequence identifiers.

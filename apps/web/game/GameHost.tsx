'use client';

export function GameHost() {
  return (
    <section aria-label="Galactic Gunners game runtime" data-game-host>
      <p>Phaser production game core will mount here through the governed game boundary.</p>
    </section>
  );
}

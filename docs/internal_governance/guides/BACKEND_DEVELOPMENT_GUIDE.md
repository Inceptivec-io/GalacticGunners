# Backend Development Guide

Django owns identity, player state, GameRun and leaderboard admission. Create schema through models+migrations. Every endpoint requires authorization/validation/error semantics and contract coverage. Use local/stage/production settings intentionally and never commit real secrets.

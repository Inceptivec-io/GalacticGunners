# Deployment Guide

Target model: feature branches merge to `dev`; accepted candidates promote to `stage`; Founder-authorised release promotes to `main`. Vercel/Railway/PostgreSQL deployment bindings are environment-specific and separately accepted. Do not equate a Git merge with a deployment or release acceptance.

# OUTSTANDING REMAINING BUILD REGISTER v1.0

Based on the supplied asset pack, Development must still make or solve the following through implementation and/or derived UI composition:

## Must be solved in code/layout
- title menu interaction system;
- sound toggle live state;
- info screen structured layout;
- HUD text/counter composition;
- Victory buttons as live actions;
- Game Over buttons as live actions;
- score/replay/nukes/lives as live values;
- touch selection feedback and pointer handling;
- proper shield tile-grid runtime reconstruction.

## May require additional Founder art later
- any future dedicated button shell variants if current icon language proves insufficient;
- any future leaderboard-specific decorative shell;
- any future Boarding-specific assets (separate workstream).

## Explicit note
Development must not block merely because every single UI state does not exist as a separate PNG. It is expected to compose the rest from the supplied visual system and code-driven events.

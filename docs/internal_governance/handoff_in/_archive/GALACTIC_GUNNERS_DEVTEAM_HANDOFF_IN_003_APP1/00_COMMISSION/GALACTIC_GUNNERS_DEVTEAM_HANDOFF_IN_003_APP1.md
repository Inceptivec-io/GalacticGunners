# GALACTIC GUNNERS
# DEVTEAM HANDOFF IN 003 APP1
## OWNED AUDIO ESTATE INTEGRATION + DOCKER FOUNDER PREVIEW

**HANDOFF ID:** `GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_003_APP1`  
**Parent Movement:** `GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_003`  
**Product:** Galactic Gunners  
**Founder Acceptance / Merge Authority:** Michael Leese

---

# 1. ENTRY COORDINATE

Repository:

```text
C:\Users\Michael\dev\GalacticGunners
```

Remote:

```text
https://github.com/Inceptivec-io/GalacticGunners.git
```

Authorised branch:

```text
feature/GG-COM-001
```

Expected exact entry HEAD:

```text
ccb80c298dd10e0c971c62f50a7c60d3b4cd614d
```

This commit is the DEVTEAM-003 returned state.

Development must independently verify:

```text
LOCAL HEAD = EXPECTED ENTRY HEAD
REMOTE FEATURE HEAD = EXPECTED ENTRY HEAD
WORKTREE = CLEAN
ACTIVE FEATURE BRANCHES = 1
```

If not:

```text
STOP — ENTRY_STATE_MISMATCH
```

Do not repair by inference.

---

# 2. PURPOSE

APP1 exists solely to add the Founder-accepted modern owned audio estate to the DEVTEAM-003 commercial reconstruction and rebuild the Docker runtime for Founder audiovisual acceptance.

This is not a new general development sprint.

---

# 3. AUTHORISED SCOPE

Authorised:

- receive/unpack this Handoff transport;
- integrate included WAV files;
- integrate included audio provenance/design documents;
- replace applicable legacy/course/uncertain runtime audio references;
- preserve legitimate unrelated audio only where still intentionally required;
- update audio preload/runtime event bindings;
- verify audio mute/sound-toggle behaviour;
- verify audio does not break existing visual/event-driven reconstruction;
- update asset/IP provenance;
- update dependency/asset/currentness/evidence registers where applicable;
- run full relevant regression;
- rebuild Docker;
- leave Docker preview ready for Founder review;
- return APP1 Handoff-Out.

Not authorised:

- new visual redesign;
- changing accepted v002 graphic art;
- changing sprite animation design except if strictly necessary to repair an audio-trigger regression;
- Boarding Mode;
- leaderboard;
- player identity;
- commercial packaging/store work;
- engine migration;
- new branch;
- merge to main;
- CLOM mutation.

---

# 4. FOUNDER AUDIO ACCEPTANCE STATE

The audio content included in this Handoff has already passed Founder audition as:

```text
ACCEPTABLE FOR RUNTIME INTEGRATION
```

This means Development may integrate it.

It does NOT mean the complete game is Founder-accepted.

After integration:

```text
FOUNDER VISUAL ACCEPTANCE = PENDING
FOUNDER FUNCTIONAL ACCEPTANCE = PENDING
FOUNDER AUDIO-IN-CONTEXT ACCEPTANCE = PENDING
```

The Founder must still preview the integrated Docker runtime.

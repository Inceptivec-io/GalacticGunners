# H015 Known Limitations

**Status:** recovery in progress.

- Existing `scripts/verify-*.mjs` browser programs are retained as `QA_DIAGNOSTIC` until individually replaced or supplemented by standard Playwright Test journeys.
- Existing evidence manifests must not call QA-driven gameplay `normal_gameplay_interaction`.
- Browser E2E coverage currently starts with public entry and splash/menu. Campaign completion, Boarding completion, Designer all-field editing, tenant isolation and production-mode negatives remain pending in the traceability register.
- No Founder review readiness is asserted by this document.

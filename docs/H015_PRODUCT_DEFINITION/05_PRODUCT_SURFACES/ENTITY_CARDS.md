# Entity Cards

`Card = projection of authoritative entity configuration.`

Cards expose class, health, speed, weapons, score, placement and boarding
capability from the same entity configuration used by the runtime. They are not
separately maintained decorative statistics.

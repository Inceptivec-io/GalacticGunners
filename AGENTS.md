# AGENTS.md — GALACTIC GUNNERS ROOT EXECUTION CONTRACT

**Document ID:** `GALACTIC_GUNNERS_ROOT_AGENTS`  
**Version:** `v1.0`  
**Scope:** Galactic Gunners repository root and all descendants unless a valid deeper `AGENTS.md` narrows execution for its subtree.  
**Repository:** `Inceptivec-io/GalacticGunners`  
**Institutional Arm:** `Inceptivec Gamification`  
**Product:** `Galactic Gunners`  
**Purpose:** Repo-local execution contract for VS Code agents, Codex/AI Workers, Development Agents, human contributors and other authorised engineering participants.

> This file is an execution/orientation contract. It is not independent institutional or constitutional authority. Current Founder direction, the active governed Handoff/Commission, applicable Secuvara/CLOM operating standards, Inceptivec authority, current repository evidence and the Galactic Gunners external boundary remain authoritative.

---

# 0. PRECEDENCE / FAIL-CLOSED

Use the following precedence:

```text
DIRECT FOUNDER AUTHORITY
+
CURRENT GOVERNED HANDOFF / COMMISSION
        ↓
APPLICABLE SECUVARA / CLOM OPERATING STANDARDS
        ↓
INCEPTIVEC / INCEPTIVEC GAMIFICATION GOVERNANCE
        ↓
GALACTIC GUNNERS PROJECT GOVERNANCE / REGISTERS
        ↓
THIS AGENTS.md
        ↓
MEMORY / CHAT CONTEXT / ASSUMPTION
```

A deeper `AGENTS.md` may narrow or refine execution for its subtree.

It may not:

- widen inherited authority;
- bypass the active Handoff;
- grant merge authority;
- override Founder decisions;
- mutate the CLOM;
- mutate another product;
- invent commercial rights;
- invent institutional structure.

If instructions conflict materially:

```text
FAIL_CLOSED
+
REPORT
```

Never derive authority merely from technical capability or file presence.

```text
MEMORY != CURRENT TRUTH
PRESENCE != AUTHORITY
HISTORICAL != CURRENT
CAPABILITY != AUTHORITY
ACCESS != PERMISSION
PASS MESSAGE != EVIDENCE
PUBLICLY AVAILABLE != COMMERCIALLY LICENSED
```

---

# 1. PRODUCT IDENTITY

Galactic Gunners is:

- a commercial Inceptivec product;
- the founding product/reference implementation of Inceptivec Gamification;
- a bounded interactive/game project;
- externally executed from the CLOM;
- governed through inherited Secuvara → Arqora → Inceptivec → Inceptivec Gamification authority.

Galactic Gunners is **not**:

- the CLOM;
- a CLOM constitutional project;
- authority to alter Secuvara governance products;
- authority to gamify GRC products;
- authority to mutate other Inceptivec/Arqora/Secuvara repositories.

The governing relationship is:

```text
SECUVARA
    ↓
ARQORA
    ↓
INCEPTIVEC
    ↓
INCEPTIVEC GAMIFICATION
    ↓
GALACTIC GUNNERS
```

Authority descends through that hierarchy.

Execution remains inside the Galactic Gunners repository and its external execution boundary.

---

# 2. CORE DOCTRINE

For existing legitimate work:

```text
PRESERVE
ENHANCE
APPEND
REFINE
```

Do not rewrite functioning historical engineering merely because newer technology exists.

Commercial modernisation must advance at least one of:

- commercial readiness;
- provenance/IP freedom;
- reliability;
- portability;
- compatibility;
- maintainability;
- accessibility;
- engagement;
- distribution;
- measurable product quality.

Do not modernise for fashion.

Do not create unnecessary frameworks or platforms.

For gaps:

```text
1. read current authority
2. inspect current implementation
3. inspect historical/provenance evidence where relevant
4. determine exact delta
5. implement smallest authorised correction
6. test
7. evidence
8. register
9. return
```

---

# 3. FIRST ACTION ON EVERY EXECUTION RUN

Before mutation, resolve and record:

```text
ACTIVE HANDOFF / COMMISSION
HANDOFF REVISION
TARGET REPOSITORY
TARGET PRODUCT / WORKSTREAM
EXECUTOR IDENTITY
AUTHORITY / SCOPE
EXCLUSIONS
LOCAL WORKING PATH
CURRENT BRANCH
ENTRY HEAD
REMOTE / UPSTREAM HEAD
GIT STATUS
DIRTY / UNTRACKED STATE
ACTIVE WORKTREE STATE
APPLICABLE AGENTS.md FILES
APPLICABLE PROJECT DOCUMENTS
APPLICABLE REGISTERS
REQUIRED TESTS
REQUIRED RETURN BOUNDARY
```

Never assume remembered:

- branch;
- SHA;
- Handoff;
- Worker identity;
- PR state;
- currentness;
- external-boundary state.

If target, authority, branch or execution surface cannot be reconciled:

```text
STOP — AUTHORITY_BOUNDARY_CONFLICT
```

---

# 4. CURRENT PRIMARY REPOSITORY

Authoritative commercial repository:

`Inceptivec-io/GalacticGunners`

Default commercial branch:

`prod`

Normal execution occurs only on the branch authorised by the current Handoff.

Do not work directly on `prod` unless Founder authority expressly commissions it.

`main` is historical/recovery-only after the governed Step 6 release cutover and retirement gates.

Founder Michael controls acceptance and merge unless explicitly delegated.

---

# 5. HISTORICAL REPOSITORY

Historical educational/provenance repository:

`michael-leese/GallacticGunners`

Default mode:

```text
READ
REVIEW
COMPARE
EVIDENCE
```

Do not mutate it during commercial development unless a future Founder-issued Handoff expressly authorises historical-repository work.

The historical repository exists to preserve:

- original educational context;
- development lineage;
- source provenance;
- historical credits;
- original testing/documentation;
- evidence of Michael Leese's early engineering work.

Do not rewrite history to make the original project appear to have been commercially created.

---

# 6. CLOM BOUNDARY

The Secuvara CLOM is not the Galactic Gunners working repository.

Where a Handoff requires reference to CLOM governance:

```text
CLOM ACCESS = READ-ONLY REFERENCE
```

unless separate explicit authority states otherwise.

Never:

- clone CLOM into Galactic Gunners;
- copy constitutional structures into the game repository without purpose;
- create new CLOM standards from this project;
- mutate CLOM registers from a Galactic Gunners execution session unless expressly authorised.

---

# 7. EXTERNAL EXECUTION BOUNDARY

Galactic Gunners uses a persistent external execution boundary.

Seed identity:

```text
_EXTERNAL_GalacticGunners\
└── _GalacticGunners_MAIN_POST_BOX\
    └── _WORK_00000001_POST_BOX\
```

The current governed specification determines the completed boundary structure.

General rules:

```text
BOUNDARY EXISTS ONCE
PAYLOADS COME AND GO
BOUNDARY REMAINS
```

A POST_BOX identity does not itself grant authority.

Direction and permitted payloads must be explicit in `BOUNDARY.md` and the boundary register.

Do not silently rename or renumber established identities.

---

# 8. HANDOFF / COMMISSION RULE

No material Development work begins without a valid current Handoff/Commission or equivalent direct Founder authority.

A Handoff defines:

- target;
- authority;
- scope;
- exclusions;
- branch;
- entry state;
- required inputs;
- required outputs;
- tests;
- evidence;
- return;
- stop conditions.

The Handoff may narrow this `AGENTS.md`.

This `AGENTS.md` may not silently widen a Handoff.

---

# 9. INBOUND RECEIVING

Inbound governed work must follow:

```text
RECEIVE
↓
PRESERVE SOURCE AS RECEIVED
↓
HASH
↓
INVENTORY
↓
RECEIVING RECORD
↓
AUTHORISED WORKING COPY
↓
SEAL INBOUND
```

Do not edit the received source package.

Do not silently repack it.

Do not use an altered copy as receiving truth.

The preserved inbound object is evidence of the authority actually received.

---

# 10. EXECUTION SURFACE

Default operating rule:

```text
ONE ACTIVE WORKSTREAM
→ ONE PRIMARY REPOSITORY
→ ONE AUTHORISED FEATURE BRANCH
→ ONE EXECUTION SESSION
→ ONE BOUNDED PRODUCT TARGET
```

Do not create:

- unnecessary clones;
- unnecessary worktrees;
- unrelated temp repositories;
- hidden working copies;
- parallel branches for the same Handoff;

unless the active Handoff explicitly permits them.

Do not contaminate other projects.

---

# 11. GIT DISCIPLINE

Before altering Git state:

- know the current branch;
- know the entry HEAD;
- know the remote HEAD;
- know staged/unstaged/untracked state;
- preserve potentially unique work.

Do not use destructive cleanup merely to obtain a clean status.

No unproved:

- `git clean`;
- hard reset;
- force checkout;
- history rewrite;
- rebase of governed evidence;
- branch deletion;
- discard of untracked work;
- force push.

Unknown unique work blocks deletion.

Investigate and classify it.

### Delivery

Authorised implementation work must normally end:

```text
TRACKED
+
COMMITTED
+
PUSHED TO AUTHORISED FEATURE BRANCH
```

unless the current Handoff explicitly requires return before push.

Founder retains merge authority.

---

# 12. COMMERCIAL IP / PROVENANCE RULE

Galactic Gunners commercialisation is deliberately provenance-first.

Every incorporated material item must be classifiable.

Authorised classifications include:

```text
OWNED
MIT / PERMISSIVELY LICENSED
ATTRIBUTION REQUIRED
COMMERCIAL USE VERIFIED
REPLACE
UNKNOWN — REPLACE / RESOLVE
```

Do not treat:

- public availability;
- Google image results;
- free download;
- historical possession;
- educational use;

as proof of commercial rights.

Where the product does not need a questionable legacy dependency:

```text
IDENTIFY
→ RECORD
→ REPLACE THROUGH AUTHORISED IP-FREEDOM WORK
```

Do not fabricate a licence.

---

# 13. RETAINED LEGITIMATE THIRD-PARTY FOUNDATION

Known legitimate retained foundation includes:

## CourseSaucerInvaders

Upstream:

`jaredyork/CourseSaucerInvaders`

Licence:

`MIT`

Required Jared York notice must be preserved.

## Phaser

Phaser is MIT licensed.

Retain required notices.

Record exact version where relevant.

Do not remove legitimate permissively licensed code merely to claim the product contains zero third-party software.

The goal is:

```text
IP FREEDOM
!=
POINTLESS REINVENTION
```

Retain legitimate foundations.

Replace unnecessary or uncertain dependencies.

---

# 14. LEGACY ASSET POLICY

Legacy non-core media with unclear or unwanted commercial dependency is scheduled for replacement.

This includes, where identified by current governance:

- legacy internet-sourced imagery;
- uncertain sprites;
- legacy fonts;
- separate nuke/fireball sound;
- uncertain utility/example code;
- unnecessary bundled documentation.

Replacement creation belongs only to an authorised workstream such as:

`IP_FREEDOM_LICENSE_PROTECTION_ASSET_CREATION`

Do not execute that work merely because `AGENTS.md` mentions it.

The current Handoff must authorise it.

---

# 15. VISUAL / PRODUCT IDENTITY PRINCIPLE

When future authorised asset creation occurs, preserve Galactic Gunners' arcade identity.

Desired direction:

```text
90s ARCADE / CONSOLE SPACE-SHOOTER MEMORY
+
CONTEMPORARY POLISH
+
MODERN SCALING / INPUT / UX
```

Avoid turning the product into:

- photorealistic space simulation;
- generic cinematic sci-fi;
- generic AI-art imagery;
- stale faux-retro imitation;
- unrelated cyberpunk styling;
- childish cartoon design.

The intended feeling is recognisably arcade and nostalgic without appearing technically old.

---

# 16. CONTRIBUTOR / HUMAN DEVELOPMENT PRINCIPLE

Galactic Gunners may provide genuine commercial-development experience to developing practitioners.

Contribution must be:

- real;
- attributable;
- evidenced;
- professionally useful;
- credited accurately.

Do not fabricate contribution.

Do not erase contribution.

Do not infer IP ownership merely from contribution.

Human-development opportunity does not remove the need for:

- commercial clarity;
- contributor rights;
- repository evidence;
- agreed compensation/royalty terms.

---

# 17. AURORA LEONARDI COMMERCIAL BOUNDARY

Aurora Leonardi is a collaborating contributor.

Known contribution includes controller/gamepad implementation/testing.

Known tested hardware includes:

- Xbox controller;
- Haute M-series controller.

Current commercial principle:

```text
Galactic Gunners Gross Receipts
− applicable tax / VAT
− platform/store commissions
− transaction fees
− refunds / chargebacks
− transparent product operating costs
= Galactic Gunners Net Royalty Base

Aurora Royalty
= 40% × Galactic Gunners Net Royalty Base
```

Operating costs must be:

- attributable to Galactic Gunners;
- transparent;
- evidenced;
- commercially reasonable;
- consistently applied.

Unrelated Secuvara/Arqora/Inceptivec costs may not be arbitrarily loaded into the product.

The royalty does not itself confer IP ownership.

Do not invent unresolved contractual terms.

---

# 18. TEST / REGRESSION RULE

A mutation is not complete because code compiles or one screen loads.

Material change lifecycle:

```text
CHANGE
→ RELEVANT TEST
→ FAILURE EVIDENCE IF ANY
→ ROOT CAUSE
→ FIX
→ RETEST
→ REGRESSION PASS
→ EVIDENCE
```

Never silently remove tests to achieve PASS.

For game/runtime changes, use the platform/device/input matrix applicable to the active stage.

Current known input families include:

- keyboard;
- touch;
- Xbox controller;
- Haute M-series controller.

Only claim test coverage actually executed.

---

# 19. PLATFORM ROADMAP BOUNDARY

Current strategic roadmap:

## Tier 1

- Web browser
- Windows
- Android
- iOS / iPadOS

## Tier 2

- macOS
- tablets
- touchscreen laptops
- desktop towers
- expanded common-controller/device coverage

## Tier 3

- Xbox feasibility
- PlayStation feasibility
- other justified console/store channels

The roadmap is not standing authority.

Do not implement a later platform merely because it appears here.

Only the current Handoff grants execution authority.

---

# 20. GG-COM PROGRAMME ORIENTATION

Current programme sequence:

```text
GG-COM-001
IP / provenance / commercial authority
        ↓
IP_FREEDOM_LICENSE_PROTECTION_ASSET_CREATION
when separately authorised
        ↓
GG-COM-002
commercial product/code baseline
        ↓
GG-COM-003
player / scoring / leaderboard
        ↓
GG-COM-004
platform / device / input assurance
        ↓
GG-COM-005
commercial packaging / legal / stores
        ↓
GG-COM-006
release / launch / validation
        ↓
GG-COM-007
evidence-led expansion
```

Programme order may be refined by Founder authority.

Do not skip ahead without commission.

---

# 21. ENGINEERING BEHAVIOUR

When implementation authority is granted, operate as a Development team rather than a conversational advisor.

Expected lifecycle:

```text
UNDERSTAND
→ VERIFY AUTHORITY
→ INSPECT
→ IMPLEMENT
→ TEST
→ FIX
→ RETEST
→ DOCUMENT
→ REGISTER
→ COMMIT
→ PUSH
→ EVIDENCE
→ HANDOFF OUT
→ SAFE EXIT
```

Do not stop at:

- plan completed;
- first file changed;
- first test passed;
- commit created;
- partial milestone.

Continue until commissioned closure criteria are met or a genuine blocker exists.

---

# 22. NO SCOPE DRIFT

Do not silently expand a Handoff.

Examples of prohibited opportunistic scope growth:

- rewriting Phaser during provenance work;
- adding Supabase during asset cleanup;
- implementing leaderboard during README work;
- redesigning art during licence review;
- creating console builds during mobile planning;
- changing other Inceptivec products because a reusable idea appears useful.

Record future opportunities as debt/roadmap proposals.

Do not execute them without authority.

---

# 23. EVIDENCE

Execution is temporary.

Evidence is durable.

Material work must leave enough evidence to determine:

```text
WHO ACTED
UNDER WHAT HANDOFF
ON WHAT REPOSITORY
FROM WHAT ENTRY SHA
ON WHAT BRANCH
WHAT CHANGED
WHY IT CHANGED
WHAT SOURCES SUPPORTED IT
WHAT WAS TESTED
WHAT PASSED / FAILED
WHAT REMAINS
WHAT EXIT SHA PROVES IT
```

Evidence must be:

- truthful;
- attributable;
- reproducible;
- proportionate;
- complete enough for independent review.

Never:

- fabricate tests;
- hide failures;
- conceal unresolved IP;
- rewrite published evidence silently;
- claim commercial clearance without provenance.

---

# 24. REGISTERS

Use the admitted Galactic Gunners external-boundary registers.

Maintain applicable concerns including:

- boundary;
- Handoff/Commission;
- Worker identity;
- Participant;
- evidence;
- asset provenance;
- dependency;
- currentness/governance debt.

If an existing register already owns the concern:

```text
UPDATE IT
```

Do not create another parallel register.

---

# 25. HISTORICAL / CURRENT SEPARATION

```text
HISTORICAL != CURRENT
PRESERVED != ACTIVE
EVIDENCE != AUTHORITY
ROADMAP != COMMISSION
```

Historical material can remain valuable.

It must not be mistaken for current commercial authority.

The commercial README, licensing position and active registers define current product state together with the live repository and active Handoff.

---

# 26. SECURITY / SECRETS

Never commit:

- passwords;
- API keys;
- access tokens;
- private signing keys;
- store credentials;
- personal financial information;
- production secrets;
- private contributor documents not intended for the repository.

Use approved environment/secret-management methods when later stages introduce services.

Do not expose connected-provider credentials in evidence.

---

# 27. COMMERCIAL / LEGAL CAUTION

Engineering records support commercial/legal review.

They do not replace competent legal/accounting advice where required.

Do not convert:

- CTO recommendation;
- Founder intent;
- unsigned royalty framework;
- draft terms;

into a claim that a signed legal agreement exists.

Record status accurately.

---

# 28. README RULE

The repository root README represents current commercial product truth.

It should remain:

- accurate;
- concise enough to navigate;
- transparent on provenance;
- current on product stage;
- clear about contributors;
- clear about third-party foundations.

Historical detail belongs in historical/provenance documentation rather than overwhelming the front page.

Do not silently revert the commercial README to its old educational-only presentation.

---

# 29. SAFE EXIT

Before ending an execution session:

```text
AUTHORISED WORK ACCOUNTED FOR
NO HIDDEN UNIQUE WORK
WORKTREE STATE KNOWN
BRANCH KNOWN
LOCAL HEAD KNOWN
REMOTE HEAD KNOWN
TEST STATE KNOWN
EVIDENCE CURRENT
REGISTERS CURRENT
HANDOFF STATE KNOWN
UNRESOLVED DEBT RECORDED
```

Do not abandon:

- untracked operational evidence;
- uncommitted authorised code;
- unpushed expected work;
- sealed packages that were subsequently edited;
- local-only changes without disclosure.

---

# 30. DEVELOPMENT RETURN / CLOSEOUT GATE

A return is prohibited until all applicable conditions are known and reported.

Return must state:

```text
HANDOFF / COMMISSION ID
EXECUTOR IDENTITY
TARGET REPOSITORY
ENTRY SHA
EXIT BRANCH
FINAL LOCAL HEAD
FINAL REMOTE HEAD
WORKTREE STATUS
FILES CHANGED
FILES REMOVED
TESTS
RESULTS
BOUNDARY STATUS
REGISTER STATUS
EVIDENCE STATUS
IP / PROVENANCE STATUS
BLOCKERS / DEBT
HANDOFF_OUT ID / PATH
HANDOFF_OUT SHA-256
CLOSURE RECOMMENDATION
```

Closure recommendation:

```text
PASS
CONDITIONAL PASS
FAIL
```

Do not return PASS while required work remains hidden or unevidenced.

---

# 31. MERGE / ACCEPTANCE

Development does not self-accept.

Development does not self-merge.

Normal boundary:

```text
DEVELOPMENT
→ IMPLEMENT / TEST / RETURN

FOUNDER MICHAEL
→ REVIEW / ACCEPT / MERGE DECISION
```

No successful test, CI result, PR status or agent confidence overrides this authority boundary.

---

# 32. FINAL EXECUTION RULE

When uncertain, ask:

```text
WHAT IS THE CURRENT HANDOFF?
WHAT IS THE EXACT TARGET?
WHAT AUTHORITY DOES IT GRANT?
WHAT DOES IT PROHIBIT?
WHAT EVIDENCE PROVES THE CURRENT STATE?
WHAT IS THE SMALLEST CORRECT ACTION?
```

Then act only inside the resolved boundary.

The objective is not merely to produce code.

The objective is to produce:

```text
COMMERCIAL PRODUCT PROGRESS
+
TRACEABLE PROVENANCE
+
CONTROLLED EXECUTION
+
REPRODUCIBLE EVIDENCE
+
CLEAN RETURN
```

# REV3 Hero Transport Disposition

Canonical runtime source: `GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_004_REV3_PACK.zip` member `PAYLOAD/assets/gg_hero_image_player_fighting.png`.

The loose POST_BOX `gg_hero_image_player_fighting.png` was also Founder-supplied but is byte-different from the pack member. REV3 pack README identifies the direct payload as `PAYLOAD/assets/gg_hero_image_player_fighting.png`; therefore the embedded pack member is used as the canonical runtime source and the loose image is recorded as a divergent duplicate transport object only.

Transport ZIP is not permanent repository evidence. Hashes/member inventory are durable evidence; canonical inspectable payload is admitted to active runtime.

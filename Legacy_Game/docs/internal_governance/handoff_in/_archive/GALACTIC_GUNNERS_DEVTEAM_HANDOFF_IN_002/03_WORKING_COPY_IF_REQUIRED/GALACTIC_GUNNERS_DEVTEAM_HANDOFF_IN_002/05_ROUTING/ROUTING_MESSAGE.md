HANDOFF / COMMISSION:
`GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_002`

PROGRAMME:
`IP_FREEDOM_LICENSE_PROTECTION_ASSET_CREATION`

TARGET:
`C:\Users\Michael\dev\GalacticGunners`

REMOTE:
`https://github.com/Inceptivec-io/GalacticGunners.git`

AUTHORISED EXECUTION BRANCH:
`feature/GG-COM-001`

DO NOT CREATE ANOTHER BRANCH.

`ACTIVE_FEATURE_BRANCHES = 1`

The Founder will supply the IP Freedom asset pack separately:

`IP_FREEDOM_LICENSE_PROTECTION_ASSET_CREATION_v1.0.zip`

That external Founder-supplied pack is the authoritative asset/audio/utility replacement payload.

Do not generate substitutes.
Do not derive alternatives.
Do not search for third-party replacements.
Do not redesign the supplied creative estate.

Before mutation:

1. read root `AGENTS.md`;
2. verify `feature/GG-COM-001`;
3. verify local HEAD == remote feature HEAD;
4. verify clean worktree;
5. record exact ENTRY_HEAD;
6. verify POST_BOX boundary-controls-only;
7. verify `docs/internal_governance` current.

Receive the separate Founder asset pack through:

`_EXTERNAL_GalacticGunners\_GalacticGunners_MAIN_POST_BOX\_WORK_00000001_POST_BOX\`

Hash, inventory, preserve immutable source under:

`docs/internal_governance/handoff_in/_archive/GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_002/`

Register it.

Then clear the transient ZIP from POST_BOX.

NO `_archive`, `handoff_in`, `handoff_out`, `evidence`, `registers`, ZIP or working payload may remain inside POST_BOX.

Implement the exact supplied replacement estate for:

- player;
- scout;
- cruiser;
- destroyer;
- boss;
- asteroid;
- comet;
- explosion;
- nuke visual;
- backgrounds;
- sound/mute;
- info;
- pause/resume/restart;
- controller/pointer;
- commercial logo/wordmark;
- symbol;
- menu;
- game-over;
- victory;
- nuke audio;
- supplied owned utility replacements;
- legacy uncertain font removal.

Use the supplied legacy→owned supersession map.

Retain legitimate MIT foundations:
- Phaser 3.16.2;
- Jared York MIT foundation;
- already verified retained MIT audio.

Do not redesign gameplay.

Only make bounded technical adjustments required for asset dimensions, frames, origins, UI position, preload paths, sound references, or accurate collision perception.

Update canonical registers only under:

`docs/internal_governance/`

No duplicate governance tree.
No duplicate living registers.

Required evidence is specified in the Handoff pack.

Run the complete runtime regression and capture integrated screenshots.

Do not begin:
- leaderboard;
- player accounts;
- Supabase;
- packaging;
- storefront work;
- console work;
- Phaser migration;
- unrelated refactor.

Do not merge `main`.

At completion return:

`GALACTIC_GUNNERS_DEVTEAM_HANDOFF_OUT_002`

Required final state:

`BRANCH = feature/GG-COM-001`
`ACTIVE_FEATURE_BRANCHES = 1`
`IP_FREEDOM_REQUIRED_ASSETS_INTEGRATED = PASS`
`LEGACY_REQUIRED_ASSETS_SUPERSEDED = PASS`
`LEGACY_UNCERTAIN_FONTS_REMOVED = PASS`
`NUKE_SOUND_REPLACED = PASS`
`PROVENANCE_REGISTER_CURRENT = PASS`
`RUNTIME_REGRESSION = PASS`
`POST_BOX_PAYLOAD = 0`
`INTERNAL_GOVERNANCE = CURRENT`
`WORKTREE = CLEAN`
`ALL_AUTHORISED_WORK = COMMITTED + PUSHED`
`LOCAL_HEAD = REMOTE feature/GG-COM-001 HEAD`
`UNTRACKED_OPERATIONAL_MATERIAL = 0`

Do not create a Git self-referential SHA loop. The returned Handoff-Out records the exact final pushed HEAD after push.

Founder Michael retains acceptance, PR finalisation and merge authority.

# GALACTIC GUNNERS
# DEVTEAM HANDOFF IN 002
## IP FREEDOM / LICENSE PROTECTION / ASSET CREATION INTEGRATION

**HANDOFF ID:** `GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_002`  
**Programme:** `IP_FREEDOM_LICENSE_PROTECTION_ASSET_CREATION`  
**Product:** `Galactic Gunners`  
**Institutional Arm:** `Inceptivec Gamification`  
**Work Level:** `PROJECT / PRODUCT`  
**Execution Type:** `Development Team Implementation Commission`  
**Founder Acceptance / Merge Authority:** `Michael Leese only`

---

# 1. AUTHORITY CHAIN

```text
SECUVARA
  ↓
ARQORA
  ↓
INCEPTIVEC
  ↓
INCEPTIVEC GAMIFICATION
  ↓
GALACTIC GUNNERS
```

Development receives implementation authority only through this Commission.

```text
CAPABILITY != AUTHORITY
ACCESS != PERMISSION
ROADMAP != COMMISSION
```

No governance, design, commercial, architectural or programme authority is delegated beyond this exact Commission and the already-admitted Galactic Gunners repository governance.

---

# 2. TARGET

```text
LOCAL REPOSITORY:
C:\Users\Michael\dev\GalacticGunners

REMOTE:
https://github.com/Inceptivec-io/GalacticGunners.git
```

Historical repository:

```text
michael-leese/GallacticGunners
```

is READ-ONLY provenance reference.

Secuvara CLOM is READ-ONLY governing reference unless separately commissioned.

---

# 3. AUTHORISED BRANCH

Continue on the existing active governed branch:

```text
feature/GG-COM-001
```

No new feature branch.

No new worktree.

No new clone.

No branch switch for execution.

No direct execution on `main`.

```text
ACTIVE_FEATURE_BRANCHES = 1
```

This remains one continuous governed workstream until Founder acceptance, PR finalisation, promotion/merge and branch closure.

---

# 4. ENTRY STATE

Before mutation, record the actual execution-time state:

```text
CURRENT BRANCH
LOCAL HEAD
REMOTE feature/GG-COM-001 HEAD
WORKTREE STATUS
POST_BOX INVENTORY
AGENTS.md STATUS
docs/internal_governance STATUS
ACTIVE HANDOFF
```

Required pre-execution gate:

```text
BRANCH = feature/GG-COM-001
WORKTREE = CLEAN
LOCAL_HEAD = REMOTE_FEATURE_HEAD
POST_BOX = BOUNDARY-CONTROLS-ONLY
AGENTS.md = TRACKED
INTERNAL_GOVERNANCE = PRESENT / CURRENT
```

The exact verified SHA at this point becomes:

```text
DEVTEAM_HANDOFF_IN_002_ENTRY_HEAD
```

Do not substitute a remembered SHA.

---

# 5. EXTERNAL AUTHORITATIVE ASSET PAYLOAD

The Founder will provide separately:

```text
IP_FREEDOM_LICENSE_PROTECTION_ASSET_CREATION_v1.0.zip
```

This asset pack is **NOT embedded in this Handoff pack**.

It is a separate Founder-supplied external payload.

Development must not create a substitute pack, alternative assets, alternative provenance package or parallel replacement estate.

The supplied external pack is the authoritative implementation payload.

---

# 6. RECEIPT OF EXTERNAL IP FREEDOM PACK

Receive the separate Founder-supplied pack only through:

```text
_EXTERNAL_GalacticGunners\
└── _GalacticGunners_MAIN_POST_BOX\
    └── _WORK_00000001_POST_BOX\
```

Required lifecycle:

```text
RECEIVE
→ HASH
→ INVENTORY
→ PRESERVE IMMUTABLE SOURCE
→ REGISTER
→ CREATE AUTHORISED WORKING COPY IF REQUIRED
→ CONSUME
→ CLEAR TRANSIENT POST_BOX PAYLOAD
```

Preserve the immutable input under:

```text
docs/internal_governance/handoff_in/_archive/
GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_002/
```

The POST_BOX must return to:

```text
BOUNDARY.md
README.md
```

at the applicable MAIN and WORK POST_BOX levels only.

No `_archive` inside POST_BOX.

No `handoff_in`.

No `handoff_out`.

No `evidence`.

No `registers`.

No ZIP remains after governed consumption.

---

# 7. COMMISSION OBJECTIVE

Integrate the exact Founder-supplied IP Freedom replacement estate into the commercial Galactic Gunners runtime.

The objective is:

```text
LEGITIMATE MIT FOUNDATION
+
INCEPTIVEC-OWNED COMMERCIAL PRESENTATION
+
INCEPTIVEC-OWNED NON-CORE ASSETS
+
INCEPTIVEC-OWNED SUPPORT UTILITIES
=
CLEAN COMMERCIAL PRODUCT ESTATE
```

Do not replace legitimate MIT foundations simply to remove third-party code.

---

# 8. REQUIRED REPLACEMENT FAMILIES

Integrate the supplied replacements for the following families:

## Core gameplay visuals

- player ship;
- scout enemy;
- cruiser enemy;
- destroyer enemy;
- boss / mothership;
- asteroid;
- comet;
- explosion;
- nuke visual effect;
- starfield/backgrounds.

## UI / presentation

- sound-on icon;
- sound-off icon;
- info/help;
- pause;
- resume;
- restart;
- controller indicator;
- pointer/focus indicator;
- Galactic Gunners logo/wordmark;
- compact symbol;
- menu/title treatment;
- game-over treatment;
- victory treatment.

## Audio

- replace legacy `nukefiring.wav` with the supplied owned nuke sound.

## Typography

- remove dependency on:
  - `assets/fonts/ARCADE.TTF`
  - `assets/fonts/ARCADEPIX.ttf`

- use the supplied proprietary wordmark for title/brand presentation;
- use only an explicitly commercially-safe/system-safe UI text solution already authorised by this Commission;
- do not download or introduce another arcade font.

## Supporting utility code

Review live call sites for:

- `assets/js/utils/UIBlock.js`
- `assets/js/utils/align.js`
- `assets/js/utils/alignGrid.js`

Replace required live behaviour using the supplied owned replacements.

Do not build a new UI framework.

---

# 9. LEGACY SUPERSESSION

Use the supersession map included in the separate Founder-supplied IP Freedom pack.

At minimum reconcile/remove/supersede the identified legacy commercial-risk families including:

```text
assets/audio/nukefiring.wav
assets/fonts/ARCADE.TTF
assets/fonts/ARCADEPIX.ttf
```

and identified legacy imagery for:

- destroyer;
- mothership;
- alien/cruiser/scout;
- asteroid;
- comet;
- dark star background;
- explosion;
- fireworks/presentation;
- sound/mute;
- info;
- pointer;
- resume;
- header/title.

Do not delete a file merely because it is old.

First determine:
- whether it is live;
- what replaces it;
- whether code references it;
- whether historical/provenance evidence must remain.

Then supersede deterministically.

---

# 10. RETAINED LEGITIMATE FOUNDATION

Retain unless a later Founder Commission says otherwise:

- Phaser `3.16.2` MIT foundation;
- Jared York MIT-covered foundation;
- exact upstream MIT-covered audio already proven during GG-COM-001.

Do not rewrite these simply because they are third-party.

```text
IP FREEDOM != POINTLESS REINVENTION
```

---

# 11. EXACT INTEGRATION METHOD

For every replacement:

1. identify the legacy runtime path;
2. identify all live code references;
3. identify current dimensions/frame expectations;
4. identify supplied replacement path;
5. calculate SHA-256 of supplied replacement;
6. create/update Asset/IP Provenance Register entry;
7. integrate replacement;
8. update preload/runtime references;
9. adjust only what is necessary for:
   - sprite dimensions;
   - animation frame counts;
   - anchors/origins;
   - safe UI positioning;
   - runtime path names;
   - sound references;
10. test affected scene immediately;
11. preserve evidence;
12. only then remove the superseded runtime asset where authorised.

Do not make broad unrelated code changes.

---

# 12. GAMEPLAY PRESERVATION

The purpose is asset/IP replacement.

Do not redesign:

- player movement;
- enemy behaviour;
- firing mechanics;
- difficulty;
- scoring;
- level sequence;
- boss logic;
- nuke mechanic;
- collision philosophy.

If a visual replacement makes current collision bounds materially inaccurate or unfair, make the smallest bounded correction and document:

```text
OLD VISUAL BOUNDS
NEW VISUAL BOUNDS
OLD COLLISION BOUNDS
NEW COLLISION BOUNDS
REASON
TEST
```

---

# 13. CREATIVE DIRECTION IS ALREADY SET

Development does not choose a new style.

The authorised direction is:

```text
90s ARCADE / CONSOLE SPACE-SHOOTER MEMORY
+
CONTEMPORARY POLISH
+
MODERN MULTI-DEVICE READABILITY
+
INCEPTIVEC-OWNED COMMERCIAL IDENTITY
```

Do not:

- search the internet for alternatives;
- use stock imagery;
- substitute AI-generated imagery;
- download fonts;
- introduce franchise-resembling assets;
- redesign the supplied logo;
- redesign the supplied sprite families.

Integrate the supplied estate.

---

# 14. PROVENANCE REQUIREMENTS

For every integrated replacement update:

```text
docs/internal_governance/registers/GG_REGISTER_ASSET_IP_PROVENANCE.md
```

Record:

- Asset ID;
- source asset pack path;
- target runtime path;
- asset type;
- creator/source;
- rights basis;
- classification;
- SHA-256;
- legacy asset superseded;
- integration status;
- evidence path.

Supplied original replacement assets are classified:

```text
OWNED / PROJECT-CREATED
```

subject to the supplied provenance evidence.

Preserve the Founder-supplied provenance register and supersession map under this Handoff evidence estate.

---

# 15. GOVERNANCE UPDATES REQUIRED

Update these canonical project surfaces:

```text
docs/internal_governance/registers/GG_REGISTER_HANDOFF_COMMISSION.md
docs/internal_governance/registers/GG_REGISTER_EVIDENCE.md
docs/internal_governance/registers/GG_REGISTER_ASSET_IP_PROVENANCE.md
docs/internal_governance/registers/GG_REGISTER_DEPENDENCY.md
docs/internal_governance/registers/GG_REGISTER_WORKER_EXECUTOR.md
docs/internal_governance/registers/GG_REGISTER_CURRENTNESS_AND_GOVERNANCE_DEBT.md
docs/internal_governance/registers/GG_REGISTER_PROJECT_STATE.md
docs/internal_governance/currentness/CURRENT_STATE.md
```

Do not create duplicate living registers.

If a register schema requires extension, extend the existing canonical register once.

---

# 16. EVIDENCE LOCATION

Durable execution evidence belongs under:

```text
docs/internal_governance/evidence/
GALACTIC_GUNNERS_DEVTEAM_HANDOFF_IN_002/
```

Include:

- inbound hash;
- pack inventory;
- legacy asset inventory;
- call-site mapping;
- replacement mapping;
- SHA-256 evidence;
- changed-file inventory;
- removed-file inventory;
- test results;
- screenshots;
- browser console results;
- controller test record;
- POST_BOX close inventory;
- Git closeout proof.

---

# 17. TESTING — REQUIRED FULL REGRESSION

After integration, verify:

## Boot/navigation

- application launch;
- main menu;
- information screen.

## Gameplay

- Level 1;
- Level 2;
- Level 3 / boss;
- player rendering;
- scout;
- cruiser;
- destroyer;
- boss;
- asteroid;
- comet;
- projectiles;
- explosion;
- nuke visual;
- nuke sound;
- pause;
- resume;
- restart;
- game over;
- victory.

## Inputs

- keyboard;
- touch where available;
- controller navigation/interaction;
- Xbox controller where available;
- Haute M-series controller where available.

If physical controller hardware is unavailable during this execution, explicitly record:

```text
HARDWARE TEST NOT EXECUTED
```

and retain existing previously proven controller baseline only where runtime controller code was untouched.

Do not claim a hardware test that did not occur.

## Visual/runtime

- no missing runtime assets;
- no 404s for required assets;
- no new browser console errors;
- sprite frames render correctly;
- no clipping;
- background behaves at desktop viewport;
- mobile viewport remains usable;
- HUD remains legible;
- menu remains readable;
- collision perception remains fair.

---

# 18. SCREENSHOT EVIDENCE

Capture integrated runtime screenshots showing at minimum:

- main menu;
- Level 1;
- Level 2;
- boss level;
- nuke effect if practicable;
- game over;
- victory.

These are execution evidence.

They do not automatically become final commercial/store screenshots.

---

# 19. PROHIBITED WORK

This Commission does NOT authorise:

- player accounts;
- leaderboard;
- score persistence;
- Supabase;
- database integration;
- cloud services;
- Windows packaging;
- Android packaging;
- iOS packaging;
- console packaging;
- storefront publishing;
- payments;
- advertising;
- commercial launch;
- Phaser migration;
- broad refactoring;
- unrelated gameplay redesign;
- historical repo mutation;
- CLOM mutation;
- `main` merge.

---

# 20. POST_BOX CLOSED STATE

At completion:

```text
_EXTERNAL_GalacticGunners\
└── _GalacticGunners_MAIN_POST_BOX\
    ├── BOUNDARY.md
    ├── README.md
    └── _WORK_00000001_POST_BOX\
        ├── BOUNDARY.md
        └── README.md
```

Exactly no active payload.

Forbidden at closure:

- `_archive`;
- `handoff_in`;
- `handoff_out`;
- `evidence`;
- `registers`;
- ZIP files;
- extraction folders;
- manifests;
- working copies.

---

# 21. GIT / BRANCH CLOSEOUT

Continue on:

```text
feature/GG-COM-001
```

All authorised work must be:

```text
TRACKED
COMMITTED
PUSHED
```

Before return:

```text
git status --porcelain
git branch --show-current
git rev-parse HEAD
git fetch origin
git rev-parse origin/feature/GG-COM-001
```

Required:

```text
BRANCH = feature/GG-COM-001
WORKTREE = CLEAN
LOCAL_HEAD = REMOTE_FEATURE_HEAD
UNTRACKED_OPERATIONAL_MATERIAL = 0
UNCOMMITTED_AUTHORISED_WORK = 0
UNPUSHED_AUTHORISED_WORK = 0
ACTIVE_FEATURE_BRANCHES = 1
```

No force push.

No self-merge.

---

# 22. CURRENTNESS SHA RULE

Do not create a self-referential Git-SHA loop.

Committed living records must distinguish:

```text
ENTRY / VERIFIED BASELINE SHA
```

from:

```text
CURRENT REMOTE HEAD AUTHORITY
=
origin/feature/GG-COM-001
```

The exact final pushed HEAD is recorded in the returned Handoff-Out/evidence after push.

Do not repeatedly commit solely to make a tracked file contain its own commit SHA.

---

# 23. HANDOFF OUT

Return:

```text
GALACTIC_GUNNERS_DEVTEAM_HANDOFF_OUT_002
```

Preserve durable outbound evidence under:

```text
docs/internal_governance/handoff_out/_archive/
GALACTIC_GUNNERS_DEVTEAM_HANDOFF_OUT_002/
```

The return must contain/report:

- Handoff ID;
- executor;
- ENTRY_HEAD;
- final branch;
- final pushed HEAD;
- remote HEAD;
- local == remote proof;
- worktree clean proof;
- active feature branch count;
- input IP Freedom pack SHA-256;
- complete integrated asset inventory;
- removed/superseded asset inventory;
- provenance register status;
- dependency register status;
- utility-code replacement status;
- test evidence;
- screenshots;
- controller hardware test status;
- browser console/error status;
- POST_BOX closed inventory;
- remaining debt;
- closure recommendation;
- sealed HANDOFF_OUT SHA-256.

---

# 24. CLOSURE GATE

PASS requires:

```text
IP_FREEDOM_REQUIRED_ASSETS_INTEGRATED = PASS
LEGACY_REQUIRED_ASSETS_SUPERSEDED = PASS
LEGACY_UNCERTAIN_FONTS_REMOVED = PASS
NUKE_SOUND_REPLACED = PASS
UTILITY_CODE_REPLACEMENT = PASS / EXPLICIT JUSTIFIED RETENTION
ASSET_PROVENANCE_REGISTER_CURRENT = PASS
DEPENDENCY_REGISTER_CURRENT = PASS
RUNTIME_REGRESSION = PASS
POST_BOX_PAYLOAD = 0
INTERNAL_GOVERNANCE_CURRENT = PASS
WORKTREE = CLEAN
ALL_AUTHORISED_WORK_COMMITTED = PASS
ALL_AUTHORISED_WORK_PUSHED = PASS
LOCAL_HEAD_EQUALS_REMOTE_HEAD = PASS
ACTIVE_FEATURE_BRANCHES = 1
UNTRACKED_OPERATIONAL_MATERIAL = 0
FOUNDER_ACCEPTANCE = PENDING
```

Development may recommend PASS.

Only Founder Michael accepts or merges.


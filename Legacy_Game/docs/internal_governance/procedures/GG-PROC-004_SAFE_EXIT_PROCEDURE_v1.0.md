# GG-PROC-004 — SAFE EXIT PROCEDURE v1.0

Run and record:

```powershell
git status --short
git branch --show-current
git rev-parse HEAD
git fetch origin
git rev-parse origin/feature/GG-COM-001
```

For the active GG-COM-001 closure, required:
- branch `feature/GG-COM-001`;
- worktree clean;
- no untracked operational material;
- local HEAD == remote feature HEAD;
- all authorised work pushed.

Inventory POST_BOX and local temporary surfaces.

PASS only when residue is zero or explicitly governed.
